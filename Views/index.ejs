<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task 8 - Chat & File Upload</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Task 8: Chat & File Upload</h1>

    <!-- Chat Section -->
    <div id="chat">
        <h2>Chat</h2>
        <div id="messages"></div>
        <form id="chatForm">
            <input type="text" id="messageInput" placeholder="Type a message" required>
            <button type="submit">Send</button>
        </form>
    </div>

    <!-- File Upload Section -->
   <!-- <div id="upload">
    <h2>Upload an Image</h2>
    <form id="uploadForm" action="/upload" method="POST" enctype="multipart/form-data">
        <input type="file" name="file" accept="image/jpeg,image/jpg,image/png" required>
        <button type="submit">Upload</button>
    </form>
    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>
    <% if (fileUrl) { %>
        <p>Uploaded Image: <a href="<%= fileUrl %>" target="_blank">View</a></p>
        <img src="<%= fileUrl %>" alt="Uploaded Image" style="max-width: 300px;">
    <% } %>
</div> -->s
    <script src="/script.js"></script>
</body>
</html>